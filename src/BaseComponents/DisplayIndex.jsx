const CountIndex = ({ index }) => {
  return (
    <span className="absolute -top-[8px] -right-[8px] text-[9px] bg-primary px-[2px] font-medium text-light rounded-full px-[5px]">
      {index}
    </span>
  );
};

export default CountIndex;