To fix: 
- delete order: bad response
- refund payment: unauthenticated (Styles de la commande aux différents états)

Next


orderSLice, update action payload


Activer ou désactiver les boutons de <Steep /> en fonction du statut de la commande
Quand le paramètre id de la commande change (useParams), il faudrait que la page se mette à jour directement.




# Think about thiiis
    # Orders frontend details
    -design orders components
    -facilite switching into order state (by right or left arrow)

    # styles for components error(404, 500)

    En voulant utiliser nextJs, il faudrait revoir la documentation kkiapay: ('use client')

    Bug: les images indicatifs (pour la pagination d'un produit) ne fonctionnent pas
    en revenant sur les produits sur l'accueil, il y a un problème d'affichage à régler

    Footer
    Image and Vidéo get from server origin (Priority: Hight)
    L'id de l'user n'est pas chargé avant l'établissement de la requête des produits.

    ## Messages
    # messages pagination
    # send message (text and image)
    # faqs configuration

# verify kkiapay response answer. For moment amount is updated directly into database